@model IEnumerable<DB.Models.Trans>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TransDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Customer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.WholesaleSign)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TransDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Customer.CustomerId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WholesaleSign)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TransID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TransID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TransID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
